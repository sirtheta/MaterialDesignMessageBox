<Window x:Class="MaterialDesignMessageBox.View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MessageBoxWindow" Height="225" Width="400" 
        WindowStartupLocation="CenterScreen" 
        WindowStyle="None" 
        Background="Transparent" 
        KeyDown="Button_KeyDown"
        AllowsTransparency="True"
        ResizeMode="NoResize" Topmost="True" ShowInTaskbar="False"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="10"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"        
        FontFamily="{DynamicResource MaterialDesignFont}">
  <Grid>
    <Border Margin="0,0,5,5" Background="White" BorderThickness="0">
      <Border.Effect>
        <DropShadowEffect BlurRadius="5" Opacity="0.4"/>
      </Border.Effect>
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="197*"/>
          <ColumnDefinition Width="295*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
          <RowDefinition Height="auto"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <materialDesign:Card x:Name="cardHeader" Grid.Row="0" Padding="10" Margin="0" materialDesign:ShadowAssist.ShadowDepth="Depth3"   Background="{DynamicResource PrimaryHueMidBrush}" Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}" Visibility="Visible" Grid.ColumnSpan="2">
          <StackPanel>
            <TextBlock x:Name="txtTitle" HorizontalAlignment="Center" VerticalAlignment="Stretch" Style="{DynamicResource MaterialDesignTitleTextBlock}" FontSize="20" >Message Title</TextBlock>
          </StackPanel>
        </materialDesign:Card>
        <Grid Grid.Row="1" Grid.ColumnSpan="2">
          <Grid Margin="5">
            <Grid.RowDefinitions>
              <RowDefinition Height="auto"/>
              <RowDefinition Height="auto"/>
              <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="0*"/>
              </Grid.RowDefinitions>

              <TextBox x:Name="txtMessage" HorizontalAlignment="Center" TextWrapping="Wrap" IsReadOnly="True" Grid.Row="0" Margin="5" materialDesign:HintAssist.Hint="" FontSize="18" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />


            </Grid>
            <Grid Grid.Row="1" Margin="0,0,0,5">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
              </Grid.ColumnDefinitions>

              <Button x:Name="btnOk" Click="BtnOk_Click" Grid.Column="1" Style="{DynamicResource MaterialDesignRaisedButton}" Margin="5" Width="100" FontSize="18" Content="OK" ToolTip="OK"/>
              <Button x:Name="btnCancel" Click="BtnCancel_Click" Grid.Column="2" Style="{DynamicResource MaterialDesignRaisedButton}" Margin="5" Width="100" FontSize="18"  Content="Cancel" ToolTip="Cancel"/>
              <Button x:Name="btnYes" Click="BtnYes_Click" Grid.Column="1" Style="{DynamicResource MaterialDesignRaisedButton}" Margin="5" Width="100" FontSize="18" Content="Yes" ToolTip="Yes"/>
              <Button x:Name="btnNo" Click="BtnNo_Click" Grid.Column="2" Style="{DynamicResource MaterialDesignRaisedButton}" Margin="5" Width="100" FontSize="18" Content="No" ToolTip="No"/>
            </Grid>
          </Grid>
        </Grid>
      </Grid>
    </Border>
  </Grid>
</Window>
